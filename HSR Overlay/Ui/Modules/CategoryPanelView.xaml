<UserControl x:Class="HSR_Overlay.Ui.Modules.CategoryPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:overlay="clr-namespace:HSR_Overlay.Util"
             Height="Auto" Width="220">
    <Border x:Name="Root"
            Margin="0"
            Background="#AA101010"
            CornerRadius="6"
            overlay:OverlayHitTest.IsInteractive="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header: drag handle -->
            <Border Grid.Row="0"
                    Background="#CC202020"
                    Padding="6,3"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="{Binding Name}"
                           FontWeight="Bold"
                           Foreground="White"/>
            </Border>

            <!-- Body: modules -->
            <Border Grid.Row="1" Background="#CC202020" Padding="4">
                <ItemsControl ItemsSource="{Binding Modules}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding ToggleCommand}"
                                    Padding="6,2"
                                    Margin="0,2"
                                    BorderThickness="0">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="#222222"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsEnabled}" Value="True">
                                                <Setter Property="Background" Value="#3355AAFF"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>

                                <Button.InputBindings>
                                    <MouseBinding MouseAction="RightClick"
                                                  Command="{Binding ShowDetailsCommand}"/>
                                </Button.InputBindings>

                                <TextBlock Text="{Binding Title}"/>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Grid>
    </Border>
</UserControl>
