<Window x:Class="HSR_Overlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HSR_Overlay"
        xmlns:overlay="clr-namespace:HSR_Overlay.Util"
        mc:Ignorable="d"
        Title="HSR Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Width="800" Height="600">
    <Grid>
        <!-- Clickable region -->
        <Border x:Name="Dropdown"
                overlay:OverlayHitTest.IsInteractive="True"
                Width="220" Height="160"
                Background="#AA111111" CornerRadius="6"
                HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="20">
            <StackPanel>
                <TextBlock Text="Demo Dropdown" Margin="8"
                           Foreground="White" FontWeight="Bold"/>
                <ComboBox x:Name="DemoCombo" Margin="8" Width="180">
                    <ComboBoxItem Content="Option A"/>
                    <ComboBoxItem Content="Option B"/>
                    <ComboBoxItem Content="Option C"/>
                </ComboBox>
            </StackPanel>
        </Border>

        <!-- Passive HUD (click-through) -->
        <TextBlock Text="Overlay HUD" Foreground="White" FontSize="18"
                   HorizontalAlignment="Right" VerticalAlignment="Top"
                   Margin="0,10,20,0" Opacity="0.9"
                   IsHitTestVisible="False"/>

        <!-- Relic popup, noninteractable-->
        <Border x:Name="RelicPopup"
          Visibility="Collapsed"
          Margin="166,0,0,0"
          HorizontalAlignment="Left"
          VerticalAlignment="Center"
          Background="#AA000000"
          CornerRadius="8"
          Padding="12" Height="96" Width="95">
            <TextBlock x:Name="RelicText"
               Text="—"
               TextWrapping="Wrap"
               MaxWidth="520"
               FontSize="16"
               Foreground="White"
               Opacity="0.95" />
        </Border>
    </Grid>
</Window>
